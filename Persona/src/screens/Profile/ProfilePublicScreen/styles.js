import {StyleSheet} from 'react-native';
import {colors} from 'resources';

const styles = StyleSheet.create({
    container: {flex: 1},
    pubilcProfileGoBackButton: {
        borderWidth: 3,
        borderColor: '#333',
        backgroundColor: 'rgba(0, 0, 0, 0.5)',
        borderRadius: 100,
        alignItems: 'center',
        justifyContent: 'center',
        padding: 5,
    },
});

export default styles;
