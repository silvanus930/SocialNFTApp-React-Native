import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        borderWidth: 0,
        borderColor: 'green',
        flexDirection: 'row',
    },
});

export default styles;
